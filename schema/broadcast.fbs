namespace uvrpc;

/* ============================================================
   Broadcast Message Format
   Replaces manual DSL format [topic_len(4), topic, data_len(4), data]
   ============================================================ */

table BroadcastMessage {
    topic: string;
    data: [ubyte];
}

root_type BroadcastMessage;